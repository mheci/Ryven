{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:best-practices",
    ":semanticCommits",
    "helpers:pinGitHubActionDigests",
  ],

  "rebaseWhen": "never",
  "dependencyDashboard": true,

  "enabledManagers": [
    "dockerfile",
    "github-actions",
    "pre-commit",
    "custom.regex",
  ],

  "dockerfile": {
    "pinDigests": true,
  },

  "pre-commit": {
    "enabled": true,
  },

  "customManagers": [
    {
      "customType": "regex",
      "description": "Pin ghcr.io/ublue-os/kinoite-main digest in Containerfile",
      "managerFilePatterns": ["/(^|/)Containerfile$/"],
      "matchStrings": [
        "FROM ghcr.io/ublue-os/kinoite-main:latest@(?<currentDigest>sha256:[a-f0-9]+)",
      ],
      "datasourceTemplate": "docker",
      "registryUrlTemplate": "https://ghcr.io",
      "depNameTemplate": "ublue-os/kinoite-main",
      "packageNameTemplate": "ublue-os/kinoite-main",
      "autoReplaceStringTemplate": "FROM ghcr.io/ublue-os/kinoite-main:latest@{{{newDigest}}}",
    },
  ],

  "packageRules": [
    {
      "automerge": true,
      "matchUpdateTypes": ["pin", "pinDigest"],
    },
    {
      "description": "Automerge digest bumps for the Ryven Containerfile base image",
      "matchManagers": ["dockerfile", "custom.regex"],
      "matchUpdateTypes": ["digest", "pinDigest"],
      "automerge": true,
    },
    {
      "automerge": true,
      "matchManagers": ["pre-commit"],
      "matchUpdateTypes": ["minor", "patch", "pin", "pinDigest"],
    },
    {
      "enabled": false,
      "matchUpdateTypes": ["digest", "pinDigest", "pin"],
      "matchDepTypes": ["container"],
      "matchFileNames": [".github/workflows/**.yaml", ".github/workflows/**.yml"],
    },
  ],
}
