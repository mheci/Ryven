# Ryven-Sericea Sway overlay (included from /etc/sway/config).
# Preferred mode = panel's advertised max res + refresh. Tearing allowed.
# Omit explicit mode so Sway uses each connector's preferred (max res/Hz).
output * {
    max_render_time off
    adaptive_sync on
    allow_tearing yes
}

# Sway has no Hyprland "master" layout; default tiling + horizontal split
# is the closest stock equivalent. Users can `layout splith`.
workspace_layout default
default_orientation horizontal
focus_follows_mouse yes
mouse_warping container

set $mod Mod4
set $term ghostty
set $menu fuzzel
set $browser helium-browser

bindsym $mod+Return exec $term
bindsym $mod+d exec $menu
bindsym $mod+b exec $browser
bindsym $mod+q kill
bindsym $mod+Shift+e exec swaynag -t warning -m 'Exit Sway?' -b 'Yes' 'swaymsg exit'
bindsym $mod+f fullscreen
bindsym $mod+Shift+space floating toggle
bindsym $mod+v exec cliphist list | fuzzel --dmenu --with-nth 2 | cliphist decode | wl-copy
bindsym $mod+Shift+x exec swaylock -f
bindsym Print exec grim -g "$(slurp)" - | satty -f - --copy-command wl-copy
bindsym $mod+Print exec grim - | wl-copy

bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right
bindsym $mod+1 workspace number 1
bindsym $mod+2 workspace number 2
bindsym $mod+3 workspace number 3
bindsym $mod+4 workspace number 4
bindsym $mod+5 workspace number 5

for_window [app_id=".*"] allow_tearing yes

exec wl-clip-persist --clipboard regular
exec wl-paste --type text --watch cliphist store
exec wl-paste --type image --watch cliphist store
exec dunst
exec /usr/libexec/kdeconnectd
exec nm-applet --indicator
exec blueman-applet
exec swaybg -i /usr/share/wallpapers/Ryven/contents/images/3840x2160.png -m fill
exec swayidle -w timeout 480 'swaymsg "output * dpms off"' resume 'swaymsg "output * dpms on"' timeout 600 'swaylock -f'
