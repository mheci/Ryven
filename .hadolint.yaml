# Hadolint for a Fedora bootc Containerfile (not Debian/Alpine).
# DL3006: base stages track floating ublue :latest tags (weekly CI rebuilds).
# DL3041: dnf version pins fight Fedora modular updates; compose uses NEVRA ladder in build.sh.
# DL3059: consecutive RUN is intentional (opt mkdir vs bind-mounted compose).
ignored:
  - DL3006
  - DL3007 # :latest bases are standing policy (weekly CI floats everything).
  - DL3041
  - DL3059
  - DL4006
failure-threshold: warning
