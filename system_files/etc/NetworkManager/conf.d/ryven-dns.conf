[main]
# Deterministic DNS architecture: NetworkManager hands per-connection DNS to
# systemd-resolved, which applies the ryven-dns.conf drop-in (Cloudflare DoT
# global resolvers, opportunistic TLS).
dns=systemd-resolved
