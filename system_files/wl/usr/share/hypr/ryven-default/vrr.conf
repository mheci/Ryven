# VRR / Adaptive Sync: fullscreen-only (matches Windows G-Sync behavior)
monitor = ,preferred,auto,1,vrr=2

# HDR / color management
misc {
    color_manager = hyprland
    vrr = 2
    enforce_hdr_capabilities = true
}
